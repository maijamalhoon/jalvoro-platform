name: Global launch dependency diagnostic

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
          show-progress: false

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 24
          cache: npm
          package-manager-cache: true

      - name: Install dependencies
        run: npm ci --silent

      - name: Capture production dependency audit
        shell: bash
        run: |
          set +e
          npm audit --omit=dev --json > global-launch-audit.json
          exit_code=$?
          echo "audit_exit_code=${exit_code}" > global-launch-audit-exit.txt
          exit 0

      - name: Upload dependency diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: global-launch-dependency-audit
          path: |
            global-launch-audit.json
            global-launch-audit-exit.txt
          if-no-files-found: error
          retention-days: 1
